"""Three executable lines, twice. Identical, and too small to say anything."""


def total_scores(values):
    result = sum(values)
    return result


def total_weights(values):
    result = sum(values)
    return result
